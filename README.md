# The-array-in-ascending-order-
Short Note:  Creates an array (list) of numbers.  Uses the sort() function to arrange elements in ascending order.  Performs in-place sorting without creating a new list.  Displays the sorted array as output.  Simple and efficient method for organizing data.
